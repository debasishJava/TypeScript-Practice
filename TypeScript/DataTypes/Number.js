function NumberTest() {
    var signed = -10;
    var unsigned = 10;
    var float = 10.40;
    var double = 10.400;
    var decimal = 10.400000000000; //upto 25 for accurate
    console.log("signed is " + signed + " \n\n    unsigned is " + unsigned + " \n\n    float is " + float + " \n\n    double is " + double + " \n\n    decimal is " + decimal);
}
NumberTest();
